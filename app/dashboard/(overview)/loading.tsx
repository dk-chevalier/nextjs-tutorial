import DashboardSkeleton from "../../ui/skeletons";

export default function Loading() {
  // loading.tsx allows us to render something while awaiting data to be fetched etc.
  return <DashboardSkeleton />;
}
